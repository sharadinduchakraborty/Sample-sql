SET SERVEROUTPUT ON
DECLARE
A NUMBER;
I NUMBER;
P NUMBER;

BEGIN
A:=&X;
I:=2;
P:=0;
WHILE I<=(A/2)
LOOP
IF(MOD(A,I)=0) THEN
	P:=1;
	EXIT;
END IF;
I:=I+1;
END LOOP;
IF(P=0) THEN
	DBMS_OUTPUT.PUT_LINE(' PRIME');
ELSE
	DBMS_OUTPUT.PUT_LINE('NOT PRIME');
END IF;

END;
/
