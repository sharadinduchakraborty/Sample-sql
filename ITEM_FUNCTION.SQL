CREATE OR REPLACE  FUNCTION  GETAMOUNT(I IN ITEM_SUBHAM.ICODE%TYPE) RETURN NUMBER IS
P NUMBER;
R NUMBER;
V NUMBER;
BEGIN 
SELECT RATE INTO R  FROM ITEM_SUBHAM WHERE ICODE=I;
V:=R*0.05;
P:=R+V+R*.05;
RETURN P;
END;
/